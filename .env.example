# Redis Configuration
REDIS_HOST=192.168.10.149
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=a

# RabbitMQ Configuration
RABBITMQ_HOST=192.168.10.149
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin
RABBITMQ_VHOST=/

# PostgreSQL Configuration
POSTGRES_HOST=192.168.10.149
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=admin

# Consul Configuration
CONSUL_HOST=192.168.10.149
CONSUL_PORT=8500

# API Gateway Configuration
API_GATEWAY_HOST=0.0.0.0
API_GATEWAY_PORT=8000
API_GATEWAY_URL=http://127.0.0.1:8000
API_GATEWAY_INTERNAL_KEY=your-secret-key
# API Keys for user authentication (comma-separated list)
# Leave empty to allow all requests (development mode only)
API_GATEWAY_API_KEYS=test-key-1,test-key-2,prod-key-abc123

# Task Scheduler Configuration
SCHEDULER_INSTANCE_ID=scheduler-001
SCHEDULER_MAX_PENDING_TASKS=2
SCHEDULER_RETRY_DELAY=2

# Model Forwarder Configuration
FORWARDER_INSTANCE_ID=forwarder-001
FORWARDER_SERVICE_HOST=0.0.0.0
FORWARDER_SERVICE_PORT=8001
FORWARDER_SERVICE_URL=http://127.0.0.1:8001

# Log Service Configuration
LOG_BATCH_SIZE=100
LOG_BATCH_TIMEOUT=5
LOG_SERVICE_HOST=0.0.0.0
LOG_SERVICE_PORT=9000
LOG_SERVICE_URL=http://127.0.0.1:9000

# Task Configuration
TASK_TTL=86400  # 24 hours in seconds
TASK_TIMEOUT=300  # 5 minutes - task processing timeout
TASK_MAX_RETRIES=3  # Maximum number of retry attempts
TASK_MAX_WAIT_TIME=120  # 120 seconds - maximum total wait time from task creation
TASK_MONITOR_INTERVAL=30  # 30 seconds - interval for checking stuck/timeout tasks

# Shared Memory Configuration
SHARED_MEMORY_ENABLED=true
SHARED_MEMORY_THRESHOLD=10240  # 10KB
SHARED_MEMORY_CLEANUP_INTERVAL=300  # 5 minutes
SHARED_MEMORY_MAX_AGE=3600  # 1 hour

# Media Processing Configuration
MEDIA_MAX_DOWNLOAD_SIZE=104857600  # 100MB
MEDIA_DOWNLOAD_TIMEOUT=60  # 60 seconds
MEDIA_MAX_FILE_SIZE=104857600  # 100MB

# MinIO Configuration
MINIO_ENDPOINT=192.168.10.149:19090
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=adminadmin
MINIO_SECURE=false
MINIO_BUCKET_INPUTS=ai-route-inputs
MINIO_BUCKET_OUTPUTS=ai-route-outputs
